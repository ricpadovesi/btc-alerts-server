services:
  - type: web
    name: btc-alerts-server
    runtime: node
    buildCommand: npm install
    startCommand: npx tsx _core/index.ts
    envVars:
      - key: NODE_ENV
        value: production
